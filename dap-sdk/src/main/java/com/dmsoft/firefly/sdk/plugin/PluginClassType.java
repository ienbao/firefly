package com.dmsoft.firefly.sdk.plugin;

/**
 * enum class for marking plugin class type
 */
public enum PluginClassType {
    DATA_PARSER, ANALYSIS, CHART, DATA_OUTPUT, RDA_PROTOCOL, CONFIG, DEFAULT
}
